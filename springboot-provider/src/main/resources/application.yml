server:
  port: 8001
spring:
  application:
    name: service-provider
  profiles:
    active: active

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka #���Զ�Ӧ����ע�����ĵ��������ݣ�ָ������ע�����ĵ�λ��
---
# java -jar xxx.jar --spring.profiles.active = dev
server:
  port: 8002
spring:
  application:
    name: service-provider
  profiles: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka